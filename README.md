# a11y-shiken

**アクセシビリティの確認は「何から手をつけるか」で止まります。**

URL を渡すと、デジタル庁『ウェブアクセシビリティ導入ガイドブック』の **基本17項目**を入口に、
必要なら WCAG 2.2 Level A + AA の **55項目**まで確認を広げる Claude Code スキルです。

ディレクターには Excel、開発者には Markdown。同じ検査結果を、それぞれが次に使える形で出します。

> ## ⚠️ v0.1.0 — ベータ版です
>
> - **正式なアクセシビリティ試験（JIS X 8341-3:2016）の代替にはなりません。** 適合表明の根拠には使わないでください
> - **55 項目のうち一部は生成 AI が判定しています。その精度は自前の検証サイトでしか測っていません**（[精度実測レポート](docs/accuracy-report.md) / [制限事項](#制限事項)）
> - HTML のみを対象とします。**PDF・動画・音声は検査できません**
> - 出力・オプション・ファイル構成は、今後の版で変わる可能性があります

---

## インストール

npm には公開していません。GitHub リポジトリから直接導入します。

### 1. `npx skills add`（推奨）

```bash
# プロジェクトに入れる
npx skills add taichiyam/a11y-shiken

# 自分の環境すべてで使う（ユーザーレベル）
npx skills add taichiyam/a11y-shiken --global
```

このリポジトリの `skills/a11y-shiken/` を読み取り、エージェントのスキルディレクトリに配置します。
インストール後、Claude Code で `/a11y-shiken` と入力すると起動します。

### 2. Claude Code plugin marketplace

`/plugin` で更新まで管理したい場合はこちらを使います。

```
/plugin marketplace add taichiyam/a11y-shiken
/plugin install a11y-shiken@a11y-shiken
```

プラグイン経由で入れた場合、起動は `/a11y-shiken:a11y-shiken` になります（プラグイン名で名前空間が付くため）。

### 動作要件

| | |
|---|---|
| [Claude Code](https://claude.com/claude-code) | スキルとして動作します |
| [bun](https://bun.sh/docs/installation) | 検査スクリプトの実行に必要です |
| Playwright Chromium | 初回実行時に自動でインストールされます |
| Windows | 未対応（ネイティブ環境は未検証。WSL2 では動作する可能性があります） |

動作確認済み: macOS 14+ / Ubuntu 22.04+

---

## 使い方

```
/a11y-shiken https://example.com
```

複数ページをまとめて検査するときは、`ラベル,URL` 形式の CSV を渡します。

```csv
TOP,https://example.com/
会社概要,https://example.com/about/
```

```
/a11y-shiken urls.csv
```

## 出てくるもの

```
docs/a11y-test/{日時}_{ドメイン}/
├── report/
│   ├── a11y-checklist-{ドメイン}-{日付}.xlsx   ← 55 行のチェックシート
│   ├── index.html                              ← サイドバー付き HTML ビューア
│   └── markdown/
│       ├── {ページ}.md                          ← HTML スニペット・推奨対応つき
│       └── {ページ}-baseline-17.md              ← 基本17項目ビュー
└── data/                                        ← 生 JSON・スクリーンショット（証跡）
```

---

## 基本17項目 — まずどこを見るか

WCAG 2.2 の55項目を渡されても、どれが致命的でどれが後回しでよいのかは判断できません。

デジタル庁『ウェブアクセシビリティ導入ガイドブック』は、この問いに公式の答えを出しています。

| | |
|---|---|
| **重大（非干渉）4項目** | できていないと、利用者がページの**他の部分にもアクセスできなくなる** |
| **必須13項目** | 満たしていないと、内容が十分に伝わらない／操作が不完全にしかできない |

このツールは、55項目の判定結果をこの**17項目に集約**して提示します。

```
| # | 項目                                 | 状態          | 内訳                        |
|---|--------------------------------------|---------------|-----------------------------|
| 2 | 袋小路に陥らせない（キーボードトラップ） | ❌ 要修正      | 1基準中 不適合 1             |
| 6 | キーボードだけで全機能にアクセスできる    | ⚠️ 一部未確認  | 5基準中 確認 3 / 未確認 2    |
```

### ⚠️ 一部しか確認できていない項目を「確認OK」に丸めません

項目6「キーボードだけで全機能にアクセスできる」は、5つの達成基準に対応します。
**3基準が適合でも、2基準が目視未確認なら「⚠️ 一部未確認」と表示します。**

ここを丸めると「間違った合格」を生みます。**間違った合格は、間違った不合格よりはるかに悪い。** 誰も後から気づかないためです。

実際の出力は [examples/komaru/](examples/komaru/) を参照してください。同じサイトの壊れた版と直った版を検査した実測結果です。

出典: [ウェブアクセシビリティ導入ガイドブック](https://www.digital.go.jp/resources/introduction-to-web-accessibility-guidebook)（デジタル庁）
本ツールはデジタル庁・WAIC・W3C・JISC の認証や推奨を受けたものではありません。

---

## 何をどう判定しているか

55 項目を、判定方法で 3 つに分けています。

| 種別 | 判定 |
|---|---|
| 🤖 **自動チェック** | [axe-core](https://github.com/dequelabs/axe-core) で検出 |
| 🔄 **部分自動チェック** | axe-core で一部検出したうえで、目視確認が必要 |
| 👁 **目視確認** | 人が判断します |

このうち、**リンクテキストが目的を説明しているかなど、文脈がないと判定できない項目**は、レンダリング後のアクセシビリティツリーと HTML を生成 AI が読んで判定します。

**証拠となる箇所を引用できないときは、合格にせず「未確認」に倒します。**

---

## 制限事項

**このツールが「できないこと」を先に書きます。**

- **正式な試験の代替になりません。** JIS X 8341-3:2016 に基づく試験は人が実施する必要があります
- **生成 AI の判定精度は、自前のダミーサイト 4 ページでしか実測していません。** 正解を伏せた盲検で独立した 3 セッションに判定させた結果、**違反を「適合」と誤判定したものは 0 件**でした。ただし生成 AI が判定を試みたのは 55 項目中 **15〜16 項目**にとどまり、仕込んだ違反 21 件のうち **8 件しか判定の対象になりませんでした**（その 8 件は 3 セッションとも検出）。機械検査と統合した最終的なチェックシートで「修正あり」になるのは **21 件中 14 件**です（[精度実測レポート](docs/accuracy-report.md)）。**「間違えない」のは「判定できることしか判定しない」ことの裏返し**であり、実サイトでは測っていないため、この数字はそのまま実サイトの精度にはなりません
- **HTML のみが対象です。** PDF・動画・音声・埋め込みコンテンツは検査できません
- **55 項目すべてが自動で埋まるわけではありません。** 目視確認が必要な項目は「未確認」として残ります
- Basic 認証つきの環境・自己署名証明書の環境には、現時点で対応していません

## 外部への送信について

**一部の判定で、検査対象ページの HTML とアクセシビリティツリーを Anthropic の API に送信します。**

Claude Code を通じて動作するため、生成 AI が判定する項目については対象ページの内容が API に送られます。axe-core・Playwright による自動チェックは、すべて手元で完結します。

**外部に出せないコンテンツを扱う場合は、この点をご確認のうえご利用ください。**

---

## ロードマップ

v0.1.0 は「動く最小構成」です。以下は未実装で、検討中のものです。

| | 内容 |
|---|---|
| 🟡 | **生成 AI 判定の精度・再現性の実測** — 自前のダミーサイト 4 ページで実測済み（[レポート](docs/accuracy-report.md)）。正解データを読んでいない状態での盲検、実サイトでの実測、独立したセッション間の再現性は未実施 |
| 🔴 | Basic 認証・自己署名証明書への対応（`httpCredentials` / `ignoreHTTPSErrors`） |
| 🟡 | ページごとの並列実行（サブエージェント化） |
| 🟢 | Google スプレッドシートへの直接出力 |

---

## ライセンスと帰属

このリポジトリは [MIT License](LICENSE) です。

**このツールは [axe-core](https://github.com/dequelabs/axe-core)（Deque Systems, Inc. / MPL-2.0）の上に成り立っています。** 自動チェックの検出そのものは axe-core が行っています。

依存ライブラリの帰属は [NOTICE](NOTICE) を参照してください。

## 参考

- [WCAG 2.2 解説書](https://waic.jp/translations/WCAG22/) — ウェブアクセシビリティ基盤委員会（WAIC）
- [ウェブアクセシビリティ導入ガイドブック](https://www.digital.go.jp/resources/introduction-to-web-accessibility-guidebook) — デジタル庁
- [駒瑠市（こまるし）](https://a11yc.com/city-komaru/) — アクセシビリティ上の問題を体験できる教材サイト（有限会社時代工房 / MIT）
